<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/WpfLib;component/themes/themedcontrol.xaml"/>
        <ResourceDictionary Source="/WpfLib;component/themes/typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--
        RadioButton, to match the square-tick CheckBox in CheckBoxStyles.

        Same 14px box metric and the same border values, so a group of radios
        and a group of checkboxes line up and read as one family. Round rather
        than square, and filled with a dot rather than a tick, because that is
        the one visual difference users rely on to tell the two apart.

        All colour values inlined inside the template (VSTO rule).
    -->

    <Style x:Key="OfficeRadioButton" TargetType="RadioButton" BasedOn="{StaticResource ThemedControl}">
        <Setter Property="FontFamily"       Value="{StaticResource UiFontFamily}"/>
        <Setter Property="FontSize"         Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="Margin"           Value="0,0,0,6"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border x:Name="Box"
                                Width="14" Height="14"
                                CornerRadius="7"
                                BorderThickness="1"
                                BorderBrush="#50808080"
                                Background="Transparent"
                                Margin="0,0,6,0">
                            <Ellipse x:Name="Dot"
                                     Width="6" Height="6"
                                     Fill="{TemplateBinding Foreground}"
                                     Opacity="0"/>
                        </Border>
                        <ContentPresenter VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Dot" Property="Opacity"     Value="1"/>
                            <Setter TargetName="Box" Property="Background"  Value="#14808080"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="#80808080"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="#80808080"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
