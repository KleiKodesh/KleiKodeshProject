<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/WpfLib;component/themes/themedcontrol.xaml"/>
        <ResourceDictionary Source="/WpfLib;component/themes/typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--
        Lists. Transparent chrome, so a list sits on the pane background rather
        than punching a white hole in a dark theme.

        The item uses the same selected and hover values as the toggles in
        ButtonStyles, including the brighter selected-and-hovered value, so a
        selected row keeps reading as selected under the cursor.

        All colour values inlined inside the templates (VSTO rule).

        VIRTUALIZATION IS ON BY DEFAULT, here and in TreeViewStyles and
        ComboBoxStyles. Recycling mode, so containers are reused rather than
        thrown away and rebuilt. CanContentScroll is stated rather than left to
        the default, because it is what makes the panel scroll by item and
        virtualize at all; turning it off for smooth pixel scrolling also needs
        VirtualizingPanel.ScrollUnit set to Pixel, or virtualization silently
        stops helping.

        Menu, ContextMenu and TabControl deliberately do NOT virtualize. They
        hold a handful of items, and virtualizing them costs keyboard
        navigation and measurement correctness for no gain.
    -->

    <Style x:Key="OfficeListBoxItem" TargetType="ListBoxItem" BasedOn="{StaticResource ThemedControl}">
        <Setter Property="Padding"          Value="8,5"/>
        <Setter Property="Background"       Value="Transparent"/>
        <Setter Property="BorderThickness"  Value="0"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FontFamily"       Value="{StaticResource UiFontFamily}"/>
        <Setter Property="FontSize"         Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#0A808080"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#3300B4FF"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected"  Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd" Property="Background" Value="#4400B4FF"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OfficeListBox" TargetType="ListBox" BasedOn="{StaticResource ThemedControl}">
        <Setter Property="Background"          Value="Transparent"/>
        <Setter Property="BorderThickness"     Value="0"/>
        <Setter Property="Padding"             Value="0"/>
        <Setter Property="FocusVisualStyle"    Value="{x:Null}"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing"     Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="ScrollViewer.CanContentScroll"        Value="True"/>
        <Setter Property="ItemContainerStyle"  Value="{StaticResource OfficeListBoxItem}"/>
    </Style>

    <!-- Bordered variant, for a list that is its own panel rather than filling one. -->
    <Style x:Key="BorderedListBox" TargetType="ListBox" BasedOn="{StaticResource OfficeListBox}">
        <Setter Property="BorderBrush"     Value="#50808080"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="2"/>
    </Style>

    <Style x:Key="OfficeListViewItem" TargetType="ListViewItem" BasedOn="{StaticResource OfficeListBoxItem}"/>

    <Style x:Key="OfficeListView" TargetType="ListView" BasedOn="{StaticResource ThemedControl}">
        <Setter Property="Background"          Value="Transparent"/>
        <Setter Property="BorderThickness"     Value="0"/>
        <Setter Property="FocusVisualStyle"    Value="{x:Null}"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing"     Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="ScrollViewer.CanContentScroll"        Value="True"/>
        <Setter Property="ItemContainerStyle"  Value="{StaticResource OfficeListViewItem}"/>
    </Style>

</ResourceDictionary>
